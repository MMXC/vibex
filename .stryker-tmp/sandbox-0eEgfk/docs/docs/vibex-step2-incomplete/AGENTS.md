# AGENTS.md: vibex-step2-incomplete

## dev (impl-step2-incomplete)

**职责**: 实现 Epic 2-4 功能

### Epic 2: ThinkingPanel 集成
- 修改 `src/app/design/bounded-context/page.tsx` 集成 ThinkingPanel
- 修改 `src/app/design/domain-model/page.tsx` 集成 ThinkingPanel
- 修改 `src/app/design/business-flow/page.tsx` 集成 ThinkingPanel
- 修改 `src/app/design/ui-generation/page.tsx` 集成 ThinkingPanel
- 验证: `npm test -- --testPathPattern="ThinkingPanel"` 通过

### Epic 3: API 持久化
- 新建 `src/app/api/design/[step]/route.ts` (GET + POST)
- 修改 `src/stores/designStore.ts` 添加 save/load
- 在每个设计页面 `page.tsx` 接入 load (mount) + save (unload)
- 验证: `curl` 手动测试 API 正常

### Epic 4: 步骤回退
- 修改 `src/stores/designStore.ts` 添加 goBack/goForward/clearForward
- 确保 StepNavigator onStepClick 接入 designStore 导航
- 验证: `npm test -- --testPathPattern="design-store"` 通过

### 约束
- 不破坏现有首页流程
- 不修改 `vibex-step2-issues` 已完成文件
- npm run build 验证通过

---

## tester (test-step2-incomplete)

**职责**: 测试验证

### 测试项
- 单元测试: `npm test -- --testPathPattern="design-store|ThinkingPanel"`
- API 测试: GET/POST /api/design/[step] 正确
- E2E: 设计流程完整路径 (刷新恢复 + 回退)

### 验收
- `npm test` 全部通过
- `npm run build` 成功
- 无 console.error

---

## reviewer (review-step2-incomplete)

**职责**: 代码审查

### 检查项
- `git diff` 检查修改范围
- designStore 状态管理正确性
- API 错误处理 (try/catch)
- ESLint + TypeScript 类型检查: `npm run lint`
- npm audit: `npm audit`

### 更新
- CHANGELOG.md 记录本次变更
- 完成后 git push

---

## 约束清单

| 约束 | 说明 |
|------|------|
| 工作目录 | /root/.openclaw/vibex/vibex-fronted |
| 测试通过 | npm test + npm run build |
| 代码风格 | ESLint + Prettier 自动格式 |
| 提交规范 | `fix(step2): <简短描述>` |
| 审查要求 | 2次 review 通过后才能合并 |

---

*Generated by: Architect Agent*
