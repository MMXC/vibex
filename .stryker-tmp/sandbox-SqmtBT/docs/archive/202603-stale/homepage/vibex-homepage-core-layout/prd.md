# PRD: 首页核心布局调整

**项目**: vibex-homepage-core-layout  
**版本**: 1.0  
**日期**: 2026-03-14  
**角色**: PM  

---

## 1. 执行摘要

**背景**: 用户核心需求 - 预览/录入分离布局，节点可勾选功能。

---

## 2. 功能需求

### F1: 预览/录入分离布局 【需页面集成】

| ID | 功能点 | 验收标准 | 优先级 |
|----|--------|----------|--------|
| F1.1 | 预览区域 60% | `expect(preview.height).toBe('60%')` | P0 |
| F1.2 | 录入区域 40% | `expect(input.height).toBe('40%')` | P0 |
| F1.3 | 固定展示 | `expect(preview).toBeFixed()` | P0 |
| F1.4 | 无 Tab 切换 | `expect(tab).not.toExist()` | P0 |

### F2: 节点勾选功能 【需页面集成】

| ID | 功能点 | 验收标准 | 优先级 |
|----|--------|----------|--------|
| F2.1 | 节点渲染 | `expect(nodes).toRender()` | P0 |
| F2.2 | 勾选控件 | `expect(checkbox).toBeVisible()` | P0 |
| F2.3 | 勾选状态 | `expect(checked).toToggle()` | P0 |
| F2.4 | 选择同步 | `expect(selection).toSync()` | P0 |

---

## 3. 目标布局结构

```
┌─────────────────────────────────────┐
│      实时预览区域 (60%)              │
│   [Mermaid 图表/节点图]              │
│   ☐ 节点1 ☐ 节点2 ☐ 节点3          │
├─────────────────────────────────────┤
│      需求录入区域 (40%)              │
│   [需求输入框 + 示例 + 开始按钮]      │
└─────────────────────────────────────┘
```

---

## 4. 验收标准

| ID | 标准 | 断言 |
|----|------|------|
| AC1 | 预览 60% | `expect(preview).toHaveHeight('60%')` |
| AC2 | 录入 40% | `expect(input).toHaveHeight('40%')` |
| AC3 | 无 Tab | `expect(tab).not.toExist()` |
| AC4 | 节点可勾选 | `expect(checkbox).toWork()` |

---

## 5. 实施计划

| 阶段 | 任务 | 工时 |
|------|------|------|
| 1 | 布局调整 | 2h |
| 2 | 节点勾选 | 2h |
| 3 | 验证 | 1h |

**总计**: 5h
