# AGENTS.md — vibex-mermaid-render-fix 开发约束

> **🎭 Coord 约束清单** — 禁止越过红线，确保交付质量

---

## 🚫 红线约束（绝对禁止）

1. **不得删除现有测试** — 不得修改 `__tests__/` 目录下的任何测试文件
2. **不得硬编码配置** — theme/securityLevel 等配置必须在 MermaidManager 中统一管理
3. **不得绕过验证流程** — 每个功能完成后必须通过检查清单验证
4. **不得移除错误处理** — 任何渲染路径都必须有 try/catch
5. **不得引入新依赖** — 不添加新的 npm 包
6. **不得降低构建质量** — `npm run build` 必须通过，0 errors

---

## 🎯 Phase 2 任务清单

### Epic 1: 统一 Mermaid 初始化管理
| ID | 功能 | 验收标准 |
|----|------|---------|
| F1.1 | MermaidManager 单例 | `expect(MermaidManager.getInstance()).toBe(MermaidManager.getInstance())` |
| F1.2 | 预初始化 | `src/app/layout.tsx` useEffect 中静默调用 `mermaidManager.initialize()` |
| F1.3 | 统一配置 | `expect(mermaid.initialize).toHaveBeenCalledWith(expect.objectContaining({theme:'dark',securityLevel:'loose'}))` |

### Epic 2: 重构 MermaidPreview 组件
| ID | 功能 | 验收标准 |
|----|------|---------|
| F2.1 | MermaidPreview 重构 | `expect(await mermaidManager.render('graph TD;A-->B')).resolves.toContain('<svg')` |
| F2.2 | 降级方案 | 传入 invalid code → `expect(screen.getByText('查看原始代码')).toBeTruthy()` |
| F2.3 | 错误消息改进 | 语法错误时 `expect(error).toMatch(/语法/)` |

### Epic 3: 清理旧组件
| ID | 功能 | 验收标准 |
|----|------|---------|
| F3.1 | 移除 MermaidRenderer 引用 | PreviewArea 中无 MermaidRenderer 引用 |
| F3.2 | 验证构建通过 | `npm run build` 成功，0 errors |

---

## 📁 工作目录
- 项目路径: `/root/.openclaw/vibex`
- 产出目录: `/root/.openclaw/vibex/docs/vibex-mermaid-render-fix/`

## ✅ DoD (Definition of Done)

每个功能完成后必须满足：
1. 对应的 expect() 断言通过
2. `npm run build` 通过
3. 无新增 TypeScript 错误
4. 改动文件已 git commit

## 🔧 页面集成标记（必须处理）
- `src/app/layout.tsx` — 预初始化调用
- `src/components/ui/MermaidPreview.tsx` — 使用 MermaidManager.render()

---

*Generated by Coord — Phase 1 Review Complete*
