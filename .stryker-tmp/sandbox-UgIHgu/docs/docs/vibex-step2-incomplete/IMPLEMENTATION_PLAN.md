# 实施计划: vibex-step2-incomplete

**项目**: vibex-step2-incomplete  
**阶段**: design-architecture  
**日期**: 2026-03-20

---

## 1. 实施范围

| Epic | 内容 | 优先级 | 工时 |
|------|------|--------|------|
| Epic 2 | ThinkingPanel 集成 | P1 | 3h |
| Epic 3 | API 持久化 | P0 | 4h |
| Epic 4 | 步骤回退 | P1 | 3h |
| 测试 | 单元 + E2E | P0 | 2h |
| **总计** | | | **12h** |

---

## 2. Epic 2: ThinkingPanel 集成 (3h)

### 2.1 实施步骤

```
Step 2.1 (1h): BoundedContextPage 集成
  - 导入 ThinkingPanel 和 useDDDStream
  - 页面挂载时初始化 SSE 连接
  - 页面卸载时 cleanup (abort)

Step 2.2 (1h): DomainModelPage 集成
  - 同上，接入 useDDDStream
  - 实体分析思考过程显示

Step 2.3 (1h): BusinessFlowPage / UIGenerationPage 集成
  - 流式输出显示
  - 进度状态 (thinking/streaming/done/error)
```

### 2.2 关键文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/app/design/bounded-context/page.tsx` | 修改 | 集成 ThinkingPanel |
| `src/app/design/domain-model/page.tsx` | 修改 | 集成 ThinkingPanel |
| `src/app/design/business-flow/page.tsx` | 修改 | 集成 ThinkingPanel |
| `src/app/design/ui-generation/page.tsx` | 修改 | 集成 ThinkingPanel |
| `src/components/ui/ThinkingPanel.tsx` | 复用 | 无需修改 |

### 2.3 验收检查清单

- [ ] bounded-context 页面有 ThinkingPanel 显示
- [ ] 流式输出实时更新面板内容
- [ ] 页面切换 SSE 连接正确关闭
- [ ] 单元测试: `ThinkingPanel.test.tsx` 通过

---

## 3. Epic 3: API 持久化 (4h)

### 3.1 实施步骤

```
Step 3.1 (1h): 创建 API 路由
  - app/api/design/[step]/route.ts
  - GET + POST 处理函数
  - 内存存储 (开发阶段)

Step 3.2 (1h): designStore 扩展
  - 添加 isDirty, isSaving, lastSavedAt
  - 实现 save(), load() actions

Step 3.3 (1h): 页面集成
  - 页面挂载时 load(step)
  - 页面卸载时 save(step) (beforeunload)
  - 数据变化时标记 isDirty

Step 3.4 (1h): 持久化配置
  - 确认 designStore persist 配置 (localStorage)
  - 添加 API 降级到 localStorage
```

### 3.2 关键文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/app/api/design/[step]/route.ts` | 新建 | API 路由 |
| `src/stores/designStore.ts` | 修改 | 添加 save/load |
| `src/app/design/*/page.tsx` | 修改 | 集成 load/save |

### 3.3 验收检查清单

- [ ] GET /api/design/[step] 返回正确数据
- [ ] POST /api/design/[step] 正确保存
- [ ] 页面刷新后数据恢复
- [ ] 单元测试: `designStore.test.ts` 通过

---

## 4. Epic 4: 步骤回退 (3h)

### 4.1 实施步骤

```
Step 4.1 (1h): designStore 回退逻辑
  - 添加 stepHistory, stepSnapshots
  - 实现 goBack(), goForward(), clearForward()

Step 4.2 (1h): StepNavigator 集成
  - 确保 onStepClick 调用 goBack/goForward
  - 限制未完成步骤不可点击

Step 4.3 (1h): 页面集成
  - 设计页面使用 designStore 导航
  - 回退时数据快照保存/恢复
```

### 4.2 关键文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/stores/designStore.ts` | 修改 | 回退逻辑 |
| `src/components/homepage/StepNavigator.tsx` | 复用 | 无需修改 |
| `src/app/design/*/page.tsx` | 修改 | 接入导航 |

### 4.3 验收检查清单

- [ ] 点击已完成步骤可回退
- [ ] 回退后数据完整保留
- [ ] 前进可恢复到回退前数据
- [ ] 回退到 Step 0 清除后续数据
- [ ] 未完成的下一步不可点击

---

## 5. 测试计划

### 5.1 单元测试

```bash
npm test -- --testPathPattern="design-store|ThinkingPanel" --passWithNoTests
```

### 5.2 API 测试

```bash
# 手动测试
curl -X POST http://localhost:3000/api/design/bounded-context \
  -H "Content-Type: application/json" \
  -d '{"contexts":[{"name":"Test"}]}'

curl http://localhost:3000/api/design/bounded-context
```

### 5.3 E2E 测试

```typescript
// tests/e2e/design-flow.spec.ts
test('设计流程完整路径', async ({ page }) => {
  await page.goto('/design/bounded-context');
  
  // 输入并分析
  await page.getByRole('textbox').fill('用户登录');
  await page.getByRole('button', { name: /分析/i }).click();
  
  // 验证 ThinkingPanel 显示
  await expect(page.getByTestId('thinking-panel')).toBeVisible();
  
  // 刷新页面
  await page.reload();
  
  // 验证数据恢复
  await expect(page.getByText('Test')).toBeVisible();
  
  // 回退到 Step 1
  await page.getByTestId('step-nav').getByText('澄清').click();
  
  // 验证数据保留
  const store = await page.evaluate(() => {
    return JSON.parse(localStorage.getItem('vibex-design') ?? '{}');
  });
  expect(store.boundedContexts).toBeDefined();
});
```

---

## 6. 风险与缓解

| 风险 | 等级 | 缓解措施 |
|------|------|----------|
| SSE 连接泄漏 | 🟡 中 | 严格 cleanup |
| 并发保存冲突 | 🟡 中 | 开发阶段用内存，暂不处理 |
| 回退数据不一致 | 🟡 中 | 快照一致性检查 |

---

## 7. 依赖关系

```
Epic 2 (ThinkingPanel) ──┐
                          ├─ Epic 4 (步骤回退)
Epic 3 (API) ────────────┘
     │
     └── 所有 Epic 共享 designStore 改动
```

---

*Generated by: Architect Agent*
