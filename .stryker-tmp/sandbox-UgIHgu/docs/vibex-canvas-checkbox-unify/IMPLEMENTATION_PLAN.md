# IMPLEMENTATION PLAN: vibex-canvas-checkbox-unify

> **Project**: vibex-canvas-checkbox-unify
> **Last Updated**: 2026-03-30

---

## Epic 1: Checkbox Toggle 修复（P0）✅

**Commit**: `96c6bf5d` - fix(canvas): toggle confirmContextNode

**Stories**:
| Story | Status | Verification |
|-------|--------|-------------|
| F1.1 Toggle 函数实现 | ✅ done | 第一次 click → confirmed=true, 第二次 → confirmed=false |
| F1.2 状态联动 | ✅ done | 取消勾选后 confirmed count 正确减少 |
| F1.3 单元测试 | ✅ done | 3 new tests in canvasStore.test.ts, all 63 tests pass |

**DoD**:
- [x] 第一次点击 confirmed: true
- [x] 第二次点击 confirmed: false
- [x] 进度正确更新

---

## Epic 2: Checkbox 语义澄清（P0）✅

**Commit**: `a81303df` - feat(canvas): Epic2 checkbox semantic clarification

**Stories**:
| Story | Status | Verification |
|-------|--------|-------------|
| F2.1 Tooltip | ✅ done | `title="用于批量选择，非确认操作"` on FlowCard checkbox |
| F2.2 工具栏删除按钮 | ✅ done (pre-existing) | `删除 ({selectedCount})` button appears when selectedCount > 0 |
| F2.3 步骤确认分离 | ✅ done (pre-existing) | FlowCard checkbox onChange only calls onToggleSelect |

**DoD**:
- [x] 工具提示正确显示
- [x] 多选后有批量操作入口
- [x] 语义分离清晰

---

## Epic 3: 分组批量确认功能（P1）✅

**Commit**: `547a4858` - feat(canvas): Epic3 组件树分组批量确认功能

**Stories**:
| Story | Status | Verification |
|-------|--------|-------------|
| F3.1 确认全部按钮 | ✅ done | `✓ 确认全部` button next to each group label |
| F3.2 批量确认逻辑 | ✅ done | `confirmAllComponentNodes(groupId)` confirms all unconfirmed in group |
| F3.3 递归确认 | ✅ done | Handles `__common__` group and multiple flowId groups |

**Files Changed**:
- `vibex-fronted/src/lib/canvas/canvasStore.ts` (+49 lines: `confirmAllComponentNodes`)
- `vibex-fronted/src/components/canvas/ComponentTree.tsx` (+17 lines: `confirmAllButton`)
- `vibex-fronted/src/components/canvas/canvas.module.css` (+24 lines: button styles)

**DoD**:
- [x] 按钮在分组标签旁
- [x] 点击后全部确认
- [x] 递归处理子分组

---

## Epic 4: Checkbox 组件统一（P1）⬜ pending

**Stories**:
| Story | Status | Blocked by |
|-------|--------|-------------|
| F4.1 状态更新回调 | ⬜ pending | reviewer-push-epic3-批量确认 |
| F4.2 CheckboxIcon 统一 | ⬜ pending | reviewer-push-epic3-批量确认 |
| F4.3 Undo/Redo 支持 | ⬜ pending | F4.1 |

**Files to Change**:
- `vibex-fronted/src/components/canvas/CheckboxIcon.tsx` (F4.2)
- `vibex-fronted/src/components/canvas/CardTreeNode.tsx` (F4.1)
- `vibex-fronted/src/lib/canvas/canvasStore.ts` (F4.1, F4.3)

---

## Verification Commands

```bash
# TypeScript check
cd vibex-fronted && npx tsc --noEmit

# Run tests
npx vitest run src/lib/canvas/canvasStore.test.ts

# Visual verification (gstack)
gstack screenshot https://vibex-app.pages.dev/canvas
```

---

*Generated by Dev Agent | Updated 2026-03-30*
