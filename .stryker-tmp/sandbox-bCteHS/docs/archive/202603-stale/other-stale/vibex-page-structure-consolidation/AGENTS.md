# AGENTS.md: vibex-page-structure-consolidation

## dev (impl-page-structure-consolidation)

### Epic 1: 路由重定向
- 新建 `src/middleware.ts` 配置 `/confirm/*` → `/` 和 `/requirements/*` → `/`
- 修改 `src/components/layout/NavBar.tsx` 移除废弃链接
- 在废弃 page.tsx 添加 `@deprecated` 注释
- **验证**: `curl -I /confirm/context` 返回 301，`npm run build` 成功

### Epic 2: Homepage 覆盖确认
- 确认并补全 Homepage 步骤组件覆盖
- 根据 specs/assessment-*.md 评估报告执行迁移决策
- **验证**: E2E 完整首页流程测试通过

### Epic 3: Design 步骤合并
- 新建 `src/components/homepage/steps/StepClarification.tsx`
- 新建 `src/components/homepage/steps/StepUIGeneration.tsx` (如评估通过)
- 集成到 Homepage 步骤流
- 确认/实现 confirmationStore ↔ designStore 同步桥
- **验证**: npm test 通过，无回归

### Epic 4: 废弃代码清理
- `rm -rf src/app/confirm/` 和 `src/app/requirements/`
- 删除废弃组件 (grep 确认无引用后)
- 更新 README 路由图
- **验证**: npm run build 成功，代码行数减少 ≥ 2000 行

### 约束
- 工作目录: /root/.openclaw/vibex/vibex-fronted
- npm run build 每次 Epic 完成后验证
- 先确认无引用再删除任何文件
- 提交规范: `refactor(page-structure): <描述>`

---

## tester (test-page-structure-consolidation)

### 测试项
- **重定向测试**: E2E 访问 /confirm/* 和 /requirements/* 重定向到 /
- **首页流程测试**: 完整首页 5 步流程
- **回归测试**: npm test 全部通过
- **构建测试**: npm run build 成功

### 验收
- E2E: 0 failed
- npm test: 0 failed
- npm run build: 成功

---

## reviewer (review-page-structure-consolidation)

### 检查项
- `git diff` 检查修改范围合理
- Middleware 重定向逻辑正确（无循环）
- 无意外删除的引用文件
- npm run lint + npm audit 无警告

### 更新
- CHANGELOG.md 记录本次重构
- 完成后 git push

---

*Generated by: Architect Agent*
