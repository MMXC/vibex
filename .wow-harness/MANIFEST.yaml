# wow-harness manifest — auto-generated by Hermes install
version: "0.1.0"
installed: "2026-04-13"
tier: adapt
hooks_installed:
  # PostToolUse
  - loop-detection         # PostToolUse[Edit|Write] — 同文件编辑 >5 次提醒
  - risk-tracker           # PostToolUse[Edit|Write] — 追踪危险工具调用频率
  # PreToolUse
  - deploy-guard           # PreToolUse[Bash] — 生产命令拦截
  - auto-python3           # PreToolUse[Bash] — python→python3 自动替换
  - review-agent-gatekeeper # PreToolUse[Task] — sub-agent 隔离审查
  - sanitize-on-read       # PreToolUse[Read|Bash] — 敏感信息脱敏
  - tool-call-counter       # PreToolUse[*] — 每 50 次工具调用目标复述
  # Stop
  - stop-evaluator         # Stop — 完成候选检测 + D8 机械化第一关
  # SessionStart
  - session-start-reset-risk   # SessionStart — 重置风险状态
  - session-start-magic-docs   # SessionStart — Magic Doc 漂移检测
  - session-start-toolkit-reminder # SessionStart — 每 3h 工具包提醒
  # SessionEnd
  - session-reflection      # SessionEnd — guard 命中统计
