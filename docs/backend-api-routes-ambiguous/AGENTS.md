# AGENTS.md: backend-api-routes-ambiguous 开发约束

**Project**: backend-api-routes-ambiguous
**Stage**: agents-constraints
**Date**: 2026-04-07

---

## 开发规范

### 强制规则

1. **必须先备份再修改**
   ```bash
   cp src/app/api/projects/\[id\]/route.ts route.ts.backup
   ```

2. **必须验证 build 无冲突后再删除 `[projectId]`**
   ```bash
   pnpm build  # 必须无 conflict/duplicate 警告
   rm -rf src/app/api/projects/\[projectId\]/
   ```

3. **必须保留 PUT/DELETE 原有行为**（不改变 API 路径，只增强安全）

---

## 文件修改规则

### Auth 检查

```typescript
// ✅ 正确：所有方法都检查 auth
const auth = getAuthUserFromRequest(request, env.JWT_SECRET);
if (!auth) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });

// ❌ 错误：遗漏 auth
const project = await prisma.project.findUnique({ where: { id } });
```

### Ownership 检查

```typescript
// ✅ 正确：PUT/DELETE 都需要 ownership 检查
const existing = await prisma.project.findUnique({ where: { id } });
if (!existing || existing.userId !== auth.userId) {
  return NextResponse.json({ error: 'Forbidden' }, { status: 403 });
}

// ❌ 错误：无 ownership 检查
await prisma.project.update({ where: { id }, data: {...} });
```

### safeError 使用

```typescript
// ✅ 正确：包含方法名和参数（脱敏）
safeError('[GET /api/projects/[id]]', error);

// ❌ 错误：原始 error 直接输出
console.error(error);
```

---

## 提交规范

```bash
# Phase 1: 合并路由
git commit -m "fix(api): merge [projectId] into [id], add auth + _count

- [id]/route.ts: GET/PUT/DELETE all require JWT auth
- GET: add _count, soft delete filter, deprecation headers
- PUT/DELETE: add ownership check (403 if not owner)
- All methods use safeError for error logging
- pnpm build: 0 conflict warnings"

# Phase 2: 测试迁移
git commit -m "test(api): migrate [projectId] tests to [id], add auth coverage

- Add GET/PUT/DELETE auth tests (AC3-AC6)
- Add _count response test (AC7)
- All tests pass: 100%"

# Phase 3: 删除 [projectId]
git commit -m "chore(api): remove [projectId] directory, route conflict resolved

- Delete src/app/api/projects/[projectId]/
- pnpm build: 0 warnings
- API path unchanged: /api/projects/:id"
```

---

## PR 清单

- [ ] `pnpm build` → 0 conflict/duplicate warnings
- [ ] `pnpm test` → all pass
- [ ] `[projectId]` 目录已删除
- [ ] GET 响应包含 `_count.pages/messages/flows`
- [ ] GET/PUT/DELETE 无 auth → 401
- [ ] PUT/DELETE 非 owner → 403
- [ ] GET 响应包含 Deprecation/Sunset Headers

---

*Generated by Architect Agent | 2026-04-07*
