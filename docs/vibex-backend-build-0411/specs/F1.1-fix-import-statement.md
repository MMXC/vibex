# Spec: F1.1 — 修复 useAIController.ts 导入语句

## 基本信息

| 字段 | 内容 |
|------|------|
| **Spec ID** | F1.1 |
| **Story ID** | S1.1 |
| **文件** | `vibex-fronted/src/hooks/canvas/useAIController.ts` |
| **修改行** | 第21行 |
| **工时** | 5 min |

## 问题描述

当前第21行：
```typescript
import { canvasSseApi } from '@/lib/canvas/api/canvasSseApi';
```

`canvasSseApi.ts` 的实际导出为：
```typescript
export const canvasSseAnalyze = async (...) => { ... };
```

不存在 `canvasSseApi` 命名空间对象，导致 TS2306 错误：
> Export "canvasSseApi" doesn't exist in target module

## 修复方案

将第21行修改为：
```typescript
import { canvasSseAnalyze } from '@/lib/canvas/api/canvasSseApi';
```

## 验收标准

```typescript
expect(source).toMatch(/import\s+\{\s*canvasSseAnalyze\s*\}/);
expect(source).not.toMatch(/import\s+\{\s*canvasSseApi\s*\}/);
```

## DoD

- [ ] 第21行导入语句已修改
- [ ] 无 TS2306 错误
