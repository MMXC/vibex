# Spec: F1.2 — 替换 canvasSseApi.canvasSseAnalyze 调用

## 基本信息

| 字段 | 内容 |
|------|------|
| **Spec ID** | F1.2 |
| **Story ID** | S1.2 |
| **文件** | `vibex-fronted/src/hooks/canvas/useAIController.ts` |
| **修改行** | 第143行附近 |
| **工时** | 2 min |
| **依赖** | F1.1 |

## 问题描述

当前调用：
```typescript
await canvasSseApi.canvasSseAnalyze(requirementInput, {...});
```

由于 `canvasSseApi` 不存在，此调用会导致运行时错误。

## 修复方案

将调用修改为：
```typescript
await canvasSseAnalyze(requirementInput, {...});
```

保持参数不变。

## 验收标准

```typescript
expect(source).toMatch(/await\s+canvasSseAnalyze\(/);
expect(source).not.toMatch(/canvasSseApi\./);
```

## DoD

- [ ] 调用已修改为 `canvasSseAnalyze(...)`
- [ ] 参数与原调用一致
