# Spec: F1.3 — 验证构建通过

## 基本信息

| 字段 | 内容 |
|------|------|
| **Spec ID** | F1.3 |
| **Story ID** | S1.3 |
| **工时** | 5 min |
| **依赖** | F1.1, F1.2 |

## 验证步骤

1. 运行 `cd vibex-fronted && npm run build`
2. 检查退出码是否为 0
3. 检查输出中是否有 TypeScript 错误

## 验收标准

```typescript
const result = execSync('cd vibex-fronted && npm run build');
expect(result.status).toBe(0);
expect(stderr).not.toMatch(/TS\d+:.*can't find/);
```

## 额外检查

- [ ] Canvas 页面在 dev 模式正常加载
- [ ] 无 Unicode 弯引号残留: `grep -rn "'" vibex-fronted/src/` 应无 JS/TS 文件匹配

## DoD

- [ ] `npm run build` 退出码为 0
- [ ] 无 TypeScript 错误输出
