# F1.1 — handleResponseError async/await 修复

**文件**: `vibex-fronted/src/lib/canvas/api/canvasApi.ts`
**行号**: 145–166
**优先级**: P0

---

## 当前有 Bug 的代码

```typescript
function handleResponseError(res: Response, defaultMsg: string, returnTo?: string): never {
  if (res.status === 401) {
    if (typeof window !== 'undefined') {
      sessionStorage.removeItem('auth_token');
      localStorage.removeItem('auth_token');
      window.dispatchEvent(new CustomEvent('auth:401', {
        detail: { returnTo: returnTo ?? window.location.pathname },
      }));
      const target = validateReturnTo(returnTo ?? window.location.pathname);
      window.location.href = `/auth?returnTo=${encodeURIComponent(target)}`;
    }
    throw new Error('登录已过期，请重新登录');
  }
  if (res.status === 404) {
    throw new Error('历史功能维护中，请稍后再试');
  }
  const err = res.json().catch(() => ({ error: `HTTP ${res.status}` }));
  throw new Error((err as { error?: string }).error ?? defaultMsg);
}
```

**Bug**: `res.json()` 返回 `Promise<any>`，`err` 永远是 Promise 对象，`(err as {...}).error` 永远是 `undefined`。

---

## 修复方案

```typescript
async function handleResponseError(res: Response, defaultMsg: string, returnTo?: string): Promise<never> {
  if (res.status === 401) {
    if (typeof window !== 'undefined') {
      sessionStorage.removeItem('auth_token');
      localStorage.removeItem('auth_token');
      window.dispatchEvent(new CustomEvent('auth:401', {
        detail: { returnTo: returnTo ?? window.location.pathname },
      }));
      const target = validateReturnTo(returnTo ?? window.location.pathname);
      window.location.href = `/auth?returnTo=${encodeURIComponent(target)}`;
    }
    throw new Error('登录已过期，请重新登录');
  }
  if (res.status === 404) {
    throw new Error('历史功能维护中，请稍后再试');
  }
  let errData: { error?: string; message?: string; details?: string } = { error: `HTTP ${res.status}` };
  try {
    errData = await res.json();
  } catch {
    // use default
  }
  throw new Error(errData.error ?? errData.message ?? errData.details ?? defaultMsg);
}
```

**改动点**:
1. 函数签名: `never` → `Promise<never>`
2. `const err = res.json().catch(...)` → `await res.json()` + try/catch
3. 错误字段优先级: `error` → `message` → `details` → `defaultMsg`

---

## 需要检查的调用方

`handleResponseError` 是同步调用的（当前在 `res.json()` 之前抛出异常的条件分支除外）：

```typescript
// 所有调用处（行号参考）：
// 128: if (!res.ok) handleResponseError(res, `API 请求失败: ${res.status}`, ...);
// 227: if (!res.ok) handleResponseError(res, `创建项目失败: ${res.status}`, ...);
// 243: if (!res.ok) handleResponseError(res, `生成失败: ${res.status}`, ...);
// 257: if (!res.ok) handleResponseError(res, `查询状态失败: ${res.status}`, ...);
// 269: if (!res.ok) handleResponseError(res, `导出失败: ${res.status}`, ...);
// 386: handleResponseError(res, `创建快照失败: ${res.status}`, ...);
// 402: if (!res.ok) handleResponseError(res, `获取快照列表失败: ${res.status}`, ...);
// 414: if (!res.ok) handleResponseError(res, `获取快照失败: ${res.status}`, ...);
// 429: if (!res.ok) handleResponseError(res, `恢复快照失败: ${res.status}`, ...);
// 444: if (!res.ok) handleResponseError(res, `获取最新版本失败: ${res.status}`, ...);
```

所有调用均在 `async` 函数内（`request` 函数本身是 `async`），因此只需确保 `await` 在调用处：

```typescript
// 修改前：
if (!res.ok) handleResponseError(res, `API 请求失败: ${res.status}`, ...);

// 修改后：
if (!res.ok) await handleResponseError(res, `API 请求失败: ${res.status}`, ...);
```

---

## 测试场景

| # | 场景 | 输入 | 预期输出 | 验证方式 |
|---|------|------|----------|----------|
| T1 | 后端返回 400 + `{ "error": "缺少必填字段 contexts" }` | HTTP 400 + body | `throw "缺少必填字段 contexts"` | mock `res.json()` 返回该对象 |
| T2 | 后端返回 400 + `{ "message": "session 已过期" }`（无 error 字段） | HTTP 400 + body | `throw "session 已过期"`（fallback 到 message） | mock `res.json()` 返回 `{ message: "..." }` |
| T3 | 后端返回 400 + `{ "details": "contexts 数组不能为空" }` | HTTP 400 + body | `throw "contexts 数组不能为空"`（fallback 到 details） | mock `res.json()` 返回 `{ details: "..." }` |
| T4 | 后端返回 400 + 空 body（JSON parse 失败） | HTTP 400 + 空 | `throw "API 请求失败: 400"`（使用 defaultMsg） | mock `res.json()` reject |
| T5 | 401 保持不变 | HTTP 401 | 仍 throw `'登录已过期，请重新登录'`，不调用 `res.json()` | 不 mock `res.json()` |
| T6 | 404 保持不变 | HTTP 404 | 仍 throw `'历史功能维护中，请稍后再试'`，不调用 `res.json()` | 不 mock `res.json()` |

---

## 验收 Checklist

- [ ] `handleResponseError` 函数签名改为 `async` + `Promise<never>`
- [ ] `res.json()` 已 `await` 并有 try/catch
- [ ] 错误优先级 `error` → `message` → `details` → `defaultMsg` 已实现
- [ ] 所有 10 处调用方已改为 `await handleResponseError(...)`
- [ ] 401/404 分支不变（不调用 `res.json()`）
- [ ] TypeScript 编译通过（`yarn typecheck`）
- [ ] 相关单元测试通过
