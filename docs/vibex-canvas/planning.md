# Planning — vibex-canvas CSS 类名解析修复

## Feature List

| ID | 功能名 | 描述 | 根因关联 | 工时估算 |
|----|--------|------|----------|----------|
| F1 | CSS 聚合层修复 | 将 `canvas.module.css` 中 `@use` 改为 `@forward`，恢复类名导出 | 直接修复根因 | 0.5h |
| F2 | 类名冲突扫描验证 | 扫描所有子模块，确认无类名冲突（同名不同值） | 防止改后引入冲突 | 0.5h |
| F3 | 样式回归验证 | Playwright 截图对比，确认 13 个组件样式恢复 | 确保改后无视觉回归 | 1.0h |
| F4 | 构建验证 | 执行 `npm run build`，确认构建通过，无 CSS 警告 | 验证产物正确性 | 0.25h |
| F5 | 部署验证 | Dev server 启动后页面正常加载，类名解析正确 | 最终验收 | 0.25h |

**总工时估算**: 2.5h

---

## Epic / Story 划分

### Epic 1: CSS 架构修复

| Story ID | 描述 | 工时 | 验收标准 |
|----------|------|------|----------|
| F1.1 | 修改 `canvas.module.css`：`@use` → `@forward` | 0.25h | `expect(canvasModule).toHaveProperty('treePanel')` |
| F1.2 | 验证所有 13 个组件类名解析正确 | 0.25h | `expect(styles.treePanel).toBeDefined()` × 13 |

### Epic 2: 验证与回归

| Story ID | 描述 | 工时 | 验收标准 |
|----------|------|------|----------|
| F2.1 | 扫描 11 个子模块类名冲突 | 0.5h | 无同名类名不同值 |
| F2.2 | Playwright 截图对比验证 | 1.0h | 截图 diff < 5% pixels changed |

### Epic 3: 构建与部署

| Story ID | 描述 | 工时 | 验收标准 |
|----------|------|------|----------|
| F3.1 | 构建验证 | 0.25h | `npm run build` exit code === 0 |
| F3.2 | Dev server 部署验证 | 0.25h | 页面可访问，类名值非 undefined |

---

## 依赖关系

```
F1.1 (修改 CSS) → F1.2 (验证类名)
                      ↓
              F2.1 (冲突扫描)
                      ↓
              F2.2 (视觉回归)
                      ↓
              F3.1 (构建验证)
                      ↓
              F3.2 (部署验证)
```

## 执行优先级

1. **P0**: F1.1, F1.2 — 根因修复，阻塞所有
2. **P1**: F3.1 — 构建通过才能交付
3. **P1**: F2.1 — 防止引入新冲突
4. **P2**: F2.2 — 视觉回归验证
5. **P2**: F3.2 — 最终部署验收
