# PRD Sketch: 首页重构 (VIBEX-006)

## 页面结构草图

```
┌─────────────────────────────────────────────────────────────┐
│                         HEADER                               │
│  Logo (左)                    Nav Links (右)                │
│  [VibeX]                      [功能] [定价] [登录]         │
├─────────────────────────────────────────────────────────────┤
│                                                          │
│                    HERO 区域 (F1.1)                       │
│                                                          │
│              🤖 AI 驱动的应用构建平台                      │
│                                                          │
│         输入需求，AI 自动生成完整应用代码                   │
│                                                          │
│     ┌─────────────────────────────────────────────┐       │
│     │  🎯 开始设计                                  │       │
│     └─────────────────────────────────────────────┘       │
│                                                          │
├─────────────────────────────────────────────────────────────┤
│                                                          │
│              PREVIEW 区域 (F1.2)  ← 固定显示              │
│     ┌─────────────────────────────────────────────┐       │
│     │                                             │       │
│     │         [AI 生成结果预览区]                   │       │
│     │                                             │       │
│     │    - 限界上下文展示                          │       │
│     │    - 领域模型图表                            │       │
│     │    - 业务流程图                              │       │
│     │                                             │       │
│     └─────────────────────────────────────────────┘       │
│                                                          │
├─────────────────────────────────────────────────────────────┤
│                                                          │
│              INPUT 区域 (F1.3)  ← 底部固定               │
│     ┌─────────────────────────────────────────────┐       │
│     │  请描述你想要的应用...            [发送]     │       │
│     └─────────────────────────────────────────────┘       │
│                                                          │
├─────────────────────────────────────────────────────────────┤
│                         FOOTER                             │
│               © 2026 VibeX. All rights reserved.         │
└─────────────────────────────────────────────────────────────┘
```

## 核心交互流程

```
用户输入需求
     │
     ▼
[AI 思考中...] ──→ 实时流式输出
     │
     ▼
生成限界上下文
     │
     ▼
生成领域模型
     │
     ▼
生成业务流程
     │
     ▼
✅ 完成 ──→ 预览区域展示
```

## 功能点映射

| 功能点 | 位置 | 说明 |
|--------|------|------|
| F1.1 | Hero | 标题 + CTA 按钮 |
| F1.2 | Preview | 中部固定预览区 |
| F1.3 | Input | 底部固定输入框 |
| F2.1 | AI 思考 | 流式思考过程显示 |
| F2.2 | 实时预览 | 边生成边显示 |

## 验收检查点

- [ ] Hero 区域标题居中显示
- [ ] "开始设计" 按钮可点击
- [ ] 预览区域固定在页面中部
- [ ] 输入框固定在页面底部
- [ ] AI 生成过程可见
- [ ] 生成结果实时展示

---

*Sketch 基于 PRD: vibex-homepage-redesign-v2*
