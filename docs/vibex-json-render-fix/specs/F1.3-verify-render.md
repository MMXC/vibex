# Spec: F1.3 — 验证预览渲染效果

## 基本信息

| 字段 | 内容 |
|------|------|
| **Spec ID** | F1.3 |
| **Story ID** | S1.3 |
| **工时** | 10 min |
| **依赖** | F1.1, F1.2 |

## 验证步骤

### Step 1: TypeScript 构建检查

```bash
cd vibex-fronted && pnpm build
```

验收标准: 退出码为 0，无 TypeScript 错误。

### Step 2: 单元测试

```bash
cd vibex-fronted && pnpm test JsonRenderPreview.test.tsx
```

验收标准: 退出码为 0，所有测试通过（预期 5 个测试）。

### Step 3: E2E 测试

```bash
cd vibex-fronted && pnpm e2e json-render-preview.spec.ts
```

验收标准: 退出码为 0，所有测试通过（预期 3 个测试）。

### Step 4: gstack browse 验证（可选）

使用 gstack browse 访问 `dev.vibex.top/canvas`，触发组件生成并打开预览，截图确认：
- Form 类型: 显示邮箱/密码字段
- List 类型: 显示 ID/名称列
- 无空白区域

## 验收标准

```typescript
// 构建
const buildResult = execSync('cd vibex-fronted && pnpm build');
expect(buildResult.status).toBe(0);

// 单元测试
const unitResult = execSync('cd vibex-fronted && pnpm test JsonRenderPreview.test.tsx');
expect(unitResult.status).toBe(0);
expect(stdout).toMatch(/5 passed/);

// E2E 测试
const e2eResult = execSync('cd vibex-fronted && pnpm e2e json-render-preview.spec.ts');
expect(e2eResult.status).toBe(0);
```

## DoD

- [ ] `pnpm build` 退出码为 0
- [ ] 单元测试 5 个全部通过
- [ ] E2E 测试 3 个全部通过
