# 实施计划: vibex-page-structure-consolidation

**项目**: vibex-page-structure-consolidation  
**阶段**: design-architecture  
**日期**: 2026-03-21

---

## 1. 实施范围

| Epic | 内容 | 优先级 | 工时 |
|------|------|--------|------|
| Epic 1 | 路由重定向 | P0 | 1天 |
| Epic 2 | Homepage 覆盖确认 | P0 | 3-5天 |
| Epic 3 | Design 步骤合并 | P1 | 5-7天 |
| Epic 4 | 废弃代码清理 | P2 | 1-2天 |
| **总计** | | | **10-15天** |

---

## 2. Epic 1: 路由重定向 (1天)

### 2.1 实施步骤

```
Day 1 AM:
  Step 1.1: 创建 middleware.ts，配置 /confirm/* → / 和 /requirements/* → /
  Step 1.2: 验证重定向 (curl -I 测试)

Day 1 PM:
  Step 1.3: 更新导航栏，移除废弃链接
  Step 1.4: 在废弃 page.tsx 添加 @deprecated 注释
  Step 1.5: E2E 重定向测试
```

### 2.2 关键文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/middleware.ts` | 新建 | 重定向中间件 |
| `src/components/layout/NavBar.tsx` | 修改 | 移除废弃链接 |
| `src/app/confirm/*/page.tsx` | 修改 | 添加 @deprecated |
| `src/app/requirements/*/page.tsx` | 修改 | 添加 @deprecated |

### 2.3 验收检查清单

- [x] `curl -I /confirm/context` 返回 301
- [x] `curl -I /requirements/list` 返回 301
- [x] 导航栏无 /confirm 和 /requirements 链接
- [x] 废弃文件含 @deprecated 注释
- [x] E2E: 访问 /confirm/* 重定向到 /

> **Epic 1 状态**: ✅ 完成 (2026-03-21) — commit 5c02c456

---

## 3. Epic 2: Homepage 覆盖确认 (3-5天)

### 3.1 实施步骤

```
Day 2-3:
  Step 2.1: 确认 StepRequirementInput 覆盖 /requirements/* 功能
  Step 2.2: 确认 StepBoundedContext 覆盖 /confirm/context 功能
  Step 2.3: 确认 StepDomainModel 覆盖 /confirm/model 功能
  Step 2.4: 确认 StepBusinessFlow 覆盖 /confirm/flow 功能

Day 4:
  Step 2.5: 评估 /design/clarification 是否需要迁移 → specs/assessment-clarification.md
  Step 2.6: 评估 /design/ui-generation 是否需要迁移 → specs/assessment-ui-generation.md

Day 5:
  Step 2.7: 根据评估结果决定迁移策略
  Step 2.8: E2E 全面覆盖测试
```

### 3.2 验收检查清单

- [x] Homepage 包含所有核心步骤组件 (Step 1-5 完整)
- [x] StepRequirementInput 覆盖 /requirements/* 功能 ✅
- [x] StepBoundedContext 覆盖 /confirm/context 功能 ✅
- [x] StepDomainModel 覆盖 /confirm/model 功能 ✅
- [x] StepBusinessFlow 覆盖 /confirm/flow 功能 ✅
- [x] /design/clarification 迁移评估完成 (recommend migrate) ✅
- [x] /design/ui-generation 迁移评估完成 (recommend keep separate) ✅
- [x] 覆盖度报告产出 → Epic2-coverage-report.md ✅

> **Epic 2 状态**: ✅ 完成 (2026-03-21)
> **下一步**: Epic 3 — 创建 StepClarification 并集成

---

## 4. Epic 3: Design 步骤合并 (5-7天)

### 4.1 实施步骤

```
Day 6-8:
  Step 3.1: 创建 StepClarification.tsx 组件 ✅
  Step 3.2: 在 Homepage 步骤流中插入 StepClarification ✅ (2026-03-21, commit af665b44)
  Step 3.3: 创建 StepUIGeneration.tsx 组件 (如评估通过) 🔄 进行中
  Step 3.4: 集成到 Homepage 步骤流
  Step 3.5: 确认 confirmationStore ↔ designStore 同步桥 ✅

Day 9-10:
  Step 3.6: 全面 E2E 测试
  Step 3.7: 回归测试 (所有现有功能)
```

### 4.2 验收检查清单

- [x] StepClarification 在 Homepage 可用 ✅ (6步流程, step 4)
- [x] Clarification 数据保存到 confirmationStore ✅ (clarificationRounds)
- [x] npm test 全部通过 ✅ (147 suites, 1674 tests)
- [ ] StepUIGeneration.tsx 组件创建
- [ ] StepUIGeneration 集成到 Homepage

---

## 5. Epic 4: 废弃代码清理 (1-2天)

### 5.1 实施步骤

```
Day 11:
  Step 4.1: 全局 grep 确认无 /confirm 和 /requirements 引用
  Step 4.2: 删除 src/app/confirm/ 目录
  Step 4.3: 删除 src/app/requirements/ 目录
  Step 4.4: npm run build 验证

Day 12:
  Step 4.5: 删除废弃组件 (ConfirmationSteps 等)
  Step 4.6: 更新 README 和路由图
  Step 4.7: git commit + 全面回归测试
```

### 5.2 验收检查清单

- [ ] grep 无 /confirm 和 /requirements 残留引用
- [ ] npm run build 成功
- [ ] npm test 全部通过
- [ ] README 更新路由图
- [ ] 代码行数减少 ≥ 2000 行

---

## 6. 测试计划

### 6.1 重定向测试

```typescript
// tests/e2e/redirect.spec.ts
test('访问 /confirm/* 重定向到 /', async ({ page }) => {
  await page.goto('/confirm/context');
  await expect(page).toHaveURL('/');
});

test('访问 /requirements/* 重定向到 /', async ({ page }) => {
  await page.goto('/requirements/list');
  await expect(page).toHaveURL('/');
});
```

### 6.2 流程完整性测试

```typescript
// tests/e2e/homepage-full-flow.spec.ts
test('完整首页流程', async ({ page }) => {
  await page.goto('/');
  
  // Step 1
  await page.getByRole('textbox').fill('用户登录系统');
  await page.getByRole('button', { name: /分析/i }).click();
  
  // Step 2
  await expect(page.getByText(/bounded context/i)).toBeVisible();
  
  // Step 3
  await page.getByRole('button', { name: /generate context/i }).click();
  await expect(page.getByText(/context/i)).toBeVisible();
  
  // 后续步骤...
  await expect(page).toHaveURL('/');
});
```

---

## 7. 风险与缓解

| 风险 | 等级 | 缓解措施 |
|------|------|----------|
| 删除有依赖的文件 | 🔴 高 | 全面 grep + build 验证 |
| 状态不一致 | 🟡 中 | 同步桥 + 单元测试 |
| 重定向循环 | 🟡 中 | Middleware 精确匹配 |
| E2E 失败 | 🟡 中 | 增量测试，每步验证 |

---

## 8. 依赖关系

```
Epic 1 (重定向) ─────────────────────────────────┐
                                                      │
Epic 2 (Homepage 确认) ───┐                        │
                           ├── Epic 3 (Design 合并) ─┤
Epic 3 (Design 合并) ────┘                        │
                                                      │
Epic 4 (代码清理) ←── 需要 Epic 1-3 全部完成 ───────┘
```

---

*Generated by: Architect Agent*
