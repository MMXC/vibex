# Feature List — vibex-sprint1-prototype-canvas

**项目**: vibex-sprint1-prototype-canvas
**日期**: 2026-04-17
**角色**: PM
**决策**: Mock数据=方案A（组件内嵌），路由树=方案A（页面列表）

---

## 决策记录

| 模糊项 | 决策 | 理由 |
|--------|------|------|
| Mock数据边界 | 采用方案 A（组件内嵌 Mock） | MVP 最简路径，工作量 0.5h；方案 B/C 留作后续迭代 |
| 路由树形态 | 采用方案 A（简单页面列表） | 最简 MVP，1h 工时；树形/DAG 留作后续迭代 |

---

## Feature List

| ID | 功能点 | 描述 | 根因关联 | 工时估算 |
|----|--------|------|---------|---------|
| **Epic 1: 拖拽布局编辑器** | | | | **4h** |
| S1.1 | 组件面板 | 左侧组件面板，展示 10 个默认组件（Button/Input/Card/Container/Header/Navigation/Modal/Table/Form/Image），支持拖拽 | J1 | 0.5h |
| S1.2 | 画布拖拽区域 | React Flow 画布，接收组件拖拽，节点可自由定位 | J1 | 2h |
| S1.3 | 自定义节点渲染 | React Flow 自定义节点，使用 UIComponent 渲染实际 UI | J1 | 1h |
| S1.4 | 节点属性面板 | 双击节点打开右侧属性面板，可编辑组件 props | J1 | 0.5h |
| **Epic 2: Mock数据绑定** | | | | **1h** |
| S2.1 | Mock数据属性Tab | 属性面板新增 "Mock数据" Tab，用户可输入/编辑内嵌 Mock JSON | J2 | 0.5h |
| S2.2 | 内嵌Mock存储与渲染 | Mock数据随组件实例保存，画布节点读取并渲染 | J2 | 0.5h |
| **Epic 3: 路由树** | | | | **1h** |
| S3.1 | 页面列表视图 | 左侧抽屉展示项目中所有页面的 route 列表（方案A） | J3 | 0.5h |
| S3.2 | 路由导航跳转 | 点击 route 列表项，画布跳转定位到对应页面节点 | J3 | 0.5h |
| **Epic 4: JSON导出增强** | | | | **1.5h** |
| S4.1 | 导出格式v2.0 | 导出 JSON 包含 version/routingTree/mockDataBindings 字段 | J4 | 1h |
| S4.2 | Round-trip验证 | 导出的 JSON 可重新导入，数据无损（含 Mock数据） | J4 | 0.5h |
| **Epic 5: 组件库完善** | | | | **0.5h** |
| S5.1 | 默认组件验证 | 验证 10 个默认组件拖入画布后渲染正确 | J1 | 0.5h |

---

## 工时汇总

| 项目 | 工时 |
|------|------|
| Epic 1 拖拽布局编辑器 | 4h |
| Epic 2 Mock数据绑定 | 1h |
| Epic 3 路由树 | 1h |
| Epic 4 JSON导出增强 | 1.5h |
| Epic 5 组件库完善 | 0.5h |
| **Total** | **8h** |

---

## 依赖关系

```
S1.1 组件面板
  └─ 依赖 S5.1 组件库完善
S1.2 画布拖拽区域
  └─ 依赖 S1.1
S1.3 自定义节点渲染
  └─ 依赖 S1.2, S2.2
S1.4 节点属性面板
  └─ 依赖 S1.2
S2.1 Mock数据属性Tab
  └─ 依赖 S1.4
S2.2 内嵌Mock存储与渲染
  └─ 依赖 S1.3
S3.1 页面列表视图
  └─ 依赖 S1.2
S3.2 路由导航跳转
  └─ 依赖 S3.1
S4.1 导出格式v2.0
  └─ 依赖 S2.2, S3.1
S4.2 Round-trip验证
  └─ 依赖 S4.1
```

---

## 后续迭代（本期不做）

| 功能 | 说明 |
|------|------|
| 方案 B: 全局 Mock Store | 全局 Mock 数据源，组件按 ID 引用 |
| 方案 C: JSON 导入 Mock | 从外部 JSON 文件导入 Mock 数据集 |
| 路由树方案 B: 树形视图 | 展示页面层级关系（父/子路由） |
| 路由树方案 C: 完整 DAG | 展示页面间所有导航关系（含条件路由） |
| 自动布局算法 | dagre/elk 自动排版 |
