# Epic 2 覆盖度确认报告

**日期**: 2026-03-21  
**执行人**: Dev Agent  
**项目**: vibex-page-structure-consolidation

---

## 1. 覆盖度检查结果

### 1.1 功能映射表

| 废弃路由 | Homepage 步骤 | 状态 | 证据 |
|---------|-------------|------|------|
| `/confirm/page.tsx` (需求输入) | `StepRequirementInput` | ✅ 完全覆盖 | 调用 `generateContexts(requirementText)` 生成限界上下文 |
| `/confirm/context` | `StepBoundedContext` | ✅ 完全覆盖 | 显示/选择 boundedContexts from confirmationStore |
| `/confirm/model` | `StepDomainModel` | ✅ 完全覆盖 | 调用 `generateDomainModels()` via useDomainModelStream |
| `/confirm/flow` | `StepBusinessFlow` | ✅ 完全覆盖 | 调用 `generateFlows()` via useBusinessFlowStream |
| `/confirm/success` | `StepProjectCreate` | ✅ 完全覆盖 | Step 5 创建项目 |
| `/requirements` | `StepRequirementInput` | ✅ 完全覆盖 | 需求输入 + 模板选择功能 |

### 1.2 步骤流程验证

```
Step 1 (RequirementInput) → generateContexts(requirementText)
    ↓ boundedContexts
Step 2 (BoundedContext) → select bounded contexts
    ↓ selectedContextIds + boundedContexts
Step 3 (DomainModel) → generateDomainModels() via useDomainModelStream
    ↓ domainModels + modelMermaidCode
Step 4 (BusinessFlow) → generateFlows() via useBusinessFlowStream
    ↓ flowMermaidCode
Step 5 (ProjectCreate) → 创建项目
    ↓ redirect / success
```

### 1.3 状态管理验证

所有步骤共享 `confirmationStore`：
- `requirementText` - Step 1 设置
- `boundedContexts` - Step 1 生成，Step 2 使用
- `selectedContextIds` - Step 2 设置
- `domainModels` + `modelMermaidCode` - Step 3 设置
- `flowMermaidCode` - Step 4 设置
- `createdProjectId` - Step 5 设置

✅ 状态流正确，步骤间数据传递无断点。

### 1.4 遗留差异

| 项目 | 状态 | 说明 |
|------|------|------|
| `/design/clarification` | ⚠️ 待迁移 | Epic 3 创建 StepClarification |
| `/design/ui-generation` | ❌ 保留独立 | 不迁移，保持 Design 工具独立 |
| `/requirements/new` | ✅ 保留 | Middleware 已排除此路由 |

---

## 2. 迁移决策确认

| 功能 | 决策 | 理由 |
|------|------|------|
| Clarification | **迁移到 Homepage** | 用户流程自然延伸，迁移成本可控 |
| UI Generation | **保留 Design 独立** | 核心护城河，架构清晰，长期可优化状态共享 |

---

## 3. Epic 3 前置条件

✅ Epic 1 重定向完成 (301 + Navbar 清理)  
✅ Epic 2 覆盖确认完成  
🔄 等待 Epic 3 开始

---

## 4. 下一步行动

- **Epic 3**: 创建 `StepClarification.tsx` 并集成到 Homepage 步骤流
  - 扩展 confirmationStore 添加 `clarificationStep`
  - 复用 `/design/clarification` 的 Chat API
  - 懒加载 (next/dynamic)
  - E2E 测试覆盖

---

*Generated by Dev Agent | 2026-03-21*
